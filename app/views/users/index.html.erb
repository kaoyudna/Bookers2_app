<div class="container">
  <div class="row">
    <div class="col-lg-3">
      <h2 class="under_line2">User infi</h2>
      <%= render 'users/info', user: current_user %>
      <h2 class="mt-3 under_line2">New book</h2>
      <%= render 'books/form', book: @book %>
    </div>
    <div class="col-lg-8 offset-md-1">
      <h2 class="under_line2">Users</h2>
      <%= link_to "グループ一覧", groups_path %> | <%= link_to "グループ作成", new_group_path %>
      <%= render 'index', users: @users %>
    </div>
  </div>
</div>