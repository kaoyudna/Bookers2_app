<div class="container">
  <div class="row">
    <div class="col-lg-8 mx-auto">
      <h4 class="text-center mt-5 mb-5"><%= @word %>に関する<%= @model %>は以下の通りです。</h4>
      <% if @model == 'user' %>
        <%= render 'users/index', users: @records %>
      <% else %>
        <%= render 'books/index', books: @records %>
      <% end %>
    </div>
  </div>
</div>